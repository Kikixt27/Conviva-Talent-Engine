"""Conviva Signal — Python pipeline (scheduled sourcing, filters, calibration).

The browser UI (`conviva_signal_v4_2.html`) stays unchanged. **Canonical batch
logic** lives in `pipeline.engine`; `scripts/source.py` is a thin forwarder.
"""
